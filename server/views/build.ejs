<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>CI build page</title>
</head>
<body>
    Navigation: <a href="/">Index page</a>

    <hr>

    <table border="1">
        <tbody>
        <tr>
            <td>ID</td>
            <td><%= task.id %></td>
        </tr>
        <tr>
            <td>Статус</td>
            <td><%= task.status %></td>
        </tr>
        <tr>
            <td>Запущен</td>
            <td><%= task.started ? (new Date(task.started)).toLocaleString() : '-' %></td>
        </tr>
        <tr>
            <td>Завершен</td>
            <td><%= task.finished ? (new Date(task.finished)).toLocaleString() : '-' %></td>
        </tr>
        <tr>
            <td>Хеш комита</td>
            <td><%= task.commitHash %></td>
        </tr>
        <tr>
            <td>Сборочная команда</td>
            <td><%= task.buildCommand %></td>
        </tr>
        <tr>
            <td>stdout</td>
            <td><pre><%= task.stdout %></pre></td>
        </tr>
        <tr>
            <td>stderr</td>
            <td><pre><%= task.stderr %></pre></td>
        </tr>
        </tbody>
    </table>
</body>
</html>
